# Data files - Raw data (large, do not push)
data/raw/*.csv
data/raw/*.xlsx

# Processed data - Allow pushing (small files, useful for reproduction)
# All processed files can be pushed (total ~1.2M)

# Models - Do NOT push (too large, share via cloud storage)
models/*.joblib
models/*.pkl

# Python
__pycache__/
*.py[cod]
*$py.class
.ipynb_checkpoints/
venv/
env/

# IDE
.vscode/
.idea/
.DS_Store

